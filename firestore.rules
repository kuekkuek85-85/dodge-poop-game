rules_version = '2';

// 이 앱은 브라우저에서 Firestore에 직접 접속하지 않는다.
// 모든 읽기·쓰기는 Vercel 서버리스 함수가 Admin SDK로 처리하며,
// Admin SDK는 이 규칙을 우회한다. 따라서 클라이언트 접근은 전면 차단한다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
